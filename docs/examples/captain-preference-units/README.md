# Fixture: captain preference units

These files are **fixtures only**.
They are not the captain's real preferences and must never be copied into a live `data/` home as if they were.

They prove the unit-plus-generated-digest pipeline end to end:

- `units/` holds preference units in the agent-harness per-unit decision form (unchanged).
- `relations/` holds one `supersedes` edge so the digest omits a replaced unit.
- `digest.md` is generated by `bin/fm-render-captain-digest.py` and must not be hand-edited.

Regenerate or check:

```sh
bin/fm-render-captain-digest.py \
  --units-dir docs/examples/captain-preference-units/units \
  --relations-dir docs/examples/captain-preference-units/relations \
  --out docs/examples/captain-preference-units/digest.md

bin/fm-render-captain-digest.py \
  --units-dir docs/examples/captain-preference-units/units \
  --relations-dir docs/examples/captain-preference-units/relations \
  --out docs/examples/captain-preference-units/digest.md \
  --check
```

Full form and home layout: [docs/captain-preference-units.md](../../captain-preference-units.md).

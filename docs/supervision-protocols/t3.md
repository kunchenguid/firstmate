Mode: T3 primary park (Cursor provider inside T3 Code).

When this session owns supervision, away mode is not active, and `state/.t3-primary-binding` is active:
1. Drain first with `bin/fm-wake-drain.sh`.
   After handling all emitted wakes and reconciling open decisions and unread status lines, run the exact `--ack-through` command printed as `WAKE_ACK_REQUIRED`; until then the work remains durable for idempotent re-handling after interruption.
2. Confirm session start bound this home (`bin/fm-t3-primary-bind.sh --status`) and that `bin/fm-t3-primary-park.sh status` reports `running`.
   If the park is stopped, run `bin/fm-t3-primary-park.sh ensure` once in the foreground (never shell `&`).
3. The park runs `bin/fm-watch-arm.sh` as a tracked child, waits for an actionable close, starts a successor arm (Option B), waits until the bound T3 thread is `ready`, then injects one operational `watcher` follow-up with `t3cli send`.
4. Ordinary wake: do not re-arm yourself; the park owns continuity.
5. Desktop Cursor `.cursor/hooks.json` stop-hook park stands down while the binding is active so the two paths do not fight.
6. AFK makes the T3 park inert; the away daemon owns triage.
7. Never use shell `&` for watcher or park supervision.
8. This is not the worker `--backend t3` spawn path; see docs/t3-primary-supervision.md.

Opt-in is local `config/t3-primary` (gitignored).
Binding lives in `state/.t3-primary-binding`.

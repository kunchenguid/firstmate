projects/
state/
data/
.no-mistakes/
.lavish/
.fm-secondmate-home
.DS_Store
__pycache__/
*.pyc
.env
# config/ holds this home's local operating choices and is private in full.
# Ignore the directory rather than each known file: an enumeration leaves every
# future config file tracked by default, so a new one would leak on the commit
# that creates it. Shipped example configs live in docs/examples/ instead.
config/

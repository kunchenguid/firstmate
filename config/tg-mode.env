# Auto-generated by fm-bootstrap.sh - Telegram mode watcher cadence.
# Source this before arming the watcher so fm-watch.sh polls the Telegram
# check every 30s. Non-TG instances have no such file and keep the
# default 300s cadence.
export FM_CHECK_INTERVAL=30

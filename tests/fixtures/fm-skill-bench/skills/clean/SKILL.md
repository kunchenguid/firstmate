---
name: clean-gh-axi
description: When about to call GitHub from a shell tool, use the wrapped helper instead of bare gh.
---

# Use gh-axi

Before any GitHub CLI operation in a worker session, invoke `gh-axi` rather than `gh`.

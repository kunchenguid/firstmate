{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc '[ -z \"${FM_TASK_ID:-}\" ] || exit 0; [ -n \"${GROK_WORKSPACE_ROOT:-}\" ] || exit 0; exec \"${GROK_WORKSPACE_ROOT:-}/bin/fm-turnend-guard-grok.sh\"'",
            "timeout": 180
          }
        ]
      }
    ]
  }
}

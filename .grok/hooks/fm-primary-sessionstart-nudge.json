{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc '[ -z \"${FM_TASK_ID:-}\" ] || exit 0; [ -n \"${GROK_WORKSPACE_ROOT:-}\" ] || exit 0; exec \"${GROK_WORKSPACE_ROOT:-}/bin/fm-sessionstart-nudge.sh\"'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}

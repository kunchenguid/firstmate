{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -lc '[ -z \"${FM_TASK_ID:-}\" ] || exit 0; [ -n \"${GROK_WORKSPACE_ROOT:-}\" ] || exit 0; exec \"${GROK_WORKSPACE_ROOT:-}/bin/fm-arm-pretool-check.sh\"'",
            "timeout": 10
          }
        ]
      }
    ]
  }
}

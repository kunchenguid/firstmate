Live theme swatch with sample prompt lines and the full ANSI ramp, in the theme's own colors. Shown in Settings and the host editor.

```jsx
<ThemePreviewCard theme="homemux-dark" />
<ThemePreviewCard theme="ember-console" showTitle={false} />
```

Theme ids: `homemux-dark`, `coastline-light`, `ember-console`, `pine-mist`.

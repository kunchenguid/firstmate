# agy (VERIFIED LEAN 2026-08-28 + PRIMARY-HARNESS PHASE 1 SHIPPED 2026-09-01, Antigravity CLI 1.1.22, Gemini harness)

Lean-registered crewmate/scout adapter; primary-harness Phase 1 shipped from scout `agy-firstmate-compatibility-scout` (report `data/agy-firstmate-compatibility-scout/report.md`).
Phase 1 ships the turn-end Stop-hook adapter, the Hard Rule 1 PreToolUse gate, and the session-start PreInvocation nudge, with a hermetic test suite (`tests/fm-turnend-guard-agy.test.sh`, 31 cases) and a clean pinned lint.
Busy/composer remain UNKNOWN (Phase 2). Herdr detects kind `agy` via `herdr:antigravity_cli` (`agent=agy`, `agent_status` idle/working); firstmate reuses it.

| Fact | Value |
|---|---|
| Launch | `agy --model <model> -i "<brief>"` prompt-interactive (`-i`/`--prompt-interactive`); brief delivered via space-free `TASK_TMP/brief.md` copy (`/tmp/fm-<id>/brief.md`) so the HOME space (`⭐️ Jala-firstmate`) never appears in the quoted `$(__OPINPUT__ encode launch-brief < __AGY_BRIEF__)` path. `--dangerously-skip-permissions` parallels claude/grok unattended. Model/effort flags are threaded from dispatch. |
| Models | `--model <model>` Gemini family (e.g. `gemini-3.7-flash-medium/high`, `gemini-3.6-flash-*`, `gemini-3.1-pro-*`) plus cross-provider ids visible via `agy models` (verified live `agy 1.1.22` lists gemini, claude-sonnet-4-6, claude-opus-4-6-thinking, gpt-oss-120b-medium). Validate against `agy models`. |
| Effort | `--effort low|medium|high` (verified via `agy --help` on 1.1.22). `xhigh`/`max` omitted (no verified flag). |
| Herdr detection | kind `agy`, source `herdr:antigravity_cli`, `agent_status` idle/working (observed w4W:p1 Lia, w53:p1 sprite-studio, w66:p1 firstmate home, w7B:p1). |
| Busy state | ROADMAP Phase 2: herdr `agent_status` (idle/working) is the native signal; wire as the semantic busy source for `harness=agy` in `bin/fm-busy-lib.sh` (herdr-native is already a classifier-only source; promote or add an `agy-herdr` source written by a probe). Until wired: UNKNOWN. |
| Composer | ROADMAP Phase 2: add the AGY composer shape to `fm_composer_classify_screen` (herdr capture: prompt line, idle banner, `agent_status`). Until wired: UNKNOWN. |
| Turn-end (primary) | SHIPPED Phase 1: `.agents/hooks.json` `"firstmate-turnend"` `Stop` hook (timeout 28800) invokes `bin/fm-turnend-guard-agy.sh` with the Stop payload on stdin. The adapter scopes via `fm-primary-scope-lib.sh` (main home and marked secondmate homes act, crew/scout task worktrees are fully inert), stands down on a live foreign session lock, away mode, or an already-healthy watcher, and otherwise runs `bin/fm-watch-arm.sh` FOREGROUND in the hook process tree (bounded attempts, default 2) and prints exactly one stdout object: `{"decision":"continue","reason":"<wake as marked operational input>"}` on an actionable wake, `{}` on healthy/silent closes, and a bounded repair continue (`FM_AGY_TURNEND_BLOCK_BUDGET`, default 3, per conversation) when the arm cannot establish a watcher. `decision: continue` is AGY's ONLY re-entry channel - no asyncRewake, no exit-2 rewake, no `loop_limit` - so continuing only on a real wake keeps the loop bounded by fleet events, never a Cursor-style park. State markers (inert to the watcher signal scan): `state/.agy-turnend-epoch` per-event record and `state/.agy-turnend-blocks` repair budget. AGY hook payloads are camelCase: `conversationId`, `fullyIdle` (AGY background tasks done - recorded, never gated, the watcher supervises the fleet), `terminationReason` (`model_stop|max_steps_exceeded|error`), `executionNum` (all verified from the installed Antigravity CLI hooks docs). |
| Hard Rule 1 | SHIPPED Phase 1: `.agents/hooks.json` `"firstmate-hardrule1"` `PreToolUse` hook (matcher `write_to_file|replace_file_content|multi_replace_file_content|run_command`, timeout 10) invokes `bin/fm-hardrule1-pretool-check.sh`, which always prints one decision object: `{"decision":"deny","reason":"Hard Rule 1: Firstmate is strictly forbidden from directly editing projects/."}` when a write-tool target (top-level or nested per-operation `TargetFile`/`Filepath`/`FilePath`/`file_path` in `toolCall.args`) or a `run_command` working directory (`args.Cwd`/`cwd`/`WorkingDirectory`, else `workspacePaths[0]`, else checkout root) resolves lexically under `<home>/projects/`; otherwise `{"decision":"allow"}`. Free-form CommandLine path arguments are deliberately out of scope so firstmate's own guarded `git -C projects/...` sync machinery is not blocked. The captain-approved escape is `FM_ALLOW_PROJECTS_WRITE=1` (mirrors `FM_ALLOW_SUBAGENT=1`). Malformed transport and an absent `projects/` directory fail open to allow. |
| Session start | SHIPPED Phase 1 (nudge tier): AGY has NO `SessionStart` hook. `.agents/hooks.json` `"firstmate-sessionstart"` `PreInvocation` hook (timeout 30) invokes `bin/fm-sessionstart-agy-nudge.sh`, which on `invocationNum == 0` runs `bin/fm-sessionstart-nudge.sh` and emits its marked `FIRSTMATE_OP: v1 session-start:` instruction as `{"injectSteps":[{"ephemeralMessage":"..."}]}`; every other invocation prints `{}`. Injection into model context is strictly better than Grok's pane-only nudge. Run-tier digest inject remains an exploratory Phase 3 item (inject-step size limit undocumented - do not attempt until measured). |
| Exit / Interrupt | Exit: `/exit` (verified clean exit to shell, Pane `unknown`); Interrupt: not yet probed - do not assume (ROADMAP Phase 5). |
| Secondmate | Not claimed - `bin/fm-spawn.sh` refuses `--secondmate` on agy (no verified primary supervision protocol yet; the Phase 1 hooks are the first brick - claim only after live probe and Phases 2-3 land). |
| Environment marker | None - detection via process ancestry `comm` name `agy` (`bin/fm-harness.sh`). No `AGY_*`/`ANTIGRAVITY_*` marker observed on live panes (OBSERVE only). Documented env: `AGY_CLI_HIDE_LOGO`, `AGY_CLI_DISABLE_ESCAPE_SEQUENCE_OPTIMIZATIONS`. |
| Session lock | AGY is NOT yet in `FM_HARNESS_RE`/`FM_HARNESS_NAMES` (`bin/fm-session-lock-lib.sh`), so `fm_session_lock_owned_by_self` and `bin/fm-lock.sh` cannot recognize an AGY session. The Stop adapter therefore uses a plain ancestry membership test like `bin/fm-sessionstart-nudge.sh` and stands down only for a live foreign lock. Adding `agy` to the harness list is a Phase 3 dependency so AGY primaries can acquire the home lock and run the full digest. |
| Skills | Same open standard (`agentskills.io`): `<workspace>/.agents/skills/<name>/SKILL.md`, global `~/.gemini/config/skills/`, `.agent/skills` back-compat, `.agents/skills.json` manifest. Frontmatter `name`/`description`/`metadata.icon`/`disable-slash-command` documented; `user-invocable`/`metadata.internal` UNDOCUMENTED - live probe required (Phase 4) before claiming 100% skill-suite compatibility. |
| Rules | `GEMINI.md` > `AGENTS.md` > `.agents/rules/*.md`; rule files limited to 12,000 chars each - firstmate's 72KB AGENTS.md may be truncated; probe and consider pointer-based AGENTS.md (Phase 6). |
| Subagents | Native `invoke_subagent`/`define_subagent`/`send_message`/`manage_subagents`; `Workspace: branch` = isolated Git worktree; context isolation; background tasks via `manage_task`/`schedule`; `Stop` hook `fullyIdle` reports background-task completion. Boundary: native subagents for fast inline research/scout only; ship/refactor work stays on external workers (`fm-spawn.sh`) with the delivery contract. |
| Hooks schema | `{ "<name>": { "enabled": bool, "<Type>": [ { "matcher": regex, "hooks": [ { "type": "command", "command": "...", "timeout": 30 } ] } ] } }`; `PreToolUse`/`PostToolUse` grouped, `PreInvocation`/`PostInvocation`/`Stop` flat. stdin/stdout JSON (camelCase, protojson); cwd = directory containing `hooks.json`; sync only; `type: "command"` only. `PreToolUse` output `decision: allow|deny|ask|force_ask`; `Stop` output `decision: "continue"` re-enters the loop with `reason` injected as a system message. The tracked registration lives in `.agents/hooks.json` and uses `bash "$PWD/bin/<script>"` because AGY sets cwd to the hooks.json directory. AGY hook commands run via `sh -c` and hook stdout must be ONE JSON object - this is why every AGY adapter always prints a decision or `{}`. |

Empirical minimum 2026-08-28 isolated Herdr lab `fm-lab-agy-lean-registe-40230-19967` w1:p1: `herdr agent start --kind agy --pane w1:p1 -- --model gemini-3.7-flash-medium -i "hello"` → `interactive_ready true`, idle; `agent prompt` accepted input (`HELLO_OK`); `/exit` returned to shell prompt (`agent_status unknown`), clean. Ctrl-C/D exit not needed.

## Phase 1 verification evidence

- `bash tests/fm-turnend-guard-agy.test.sh` → 31/31 passing, exit 0: payload parsing (empty/malformed/mistyped stand down), primary scope vs inert task worktree vs marked secondmate home, supervision-needed gating, watcher-healthy skip, away mode, foreign/dead session locks, foreground arming with wake delivery as one `continue`, bounded repair nag (3 per conversation) then silence, wake resets the repair budget, epoch-marker metadata, Hard Rule 1 deny/allow matrix (absolute, relative, nested multi-edit, aliases, `run_command` cwd, workspace fallback, `..` normalization, absent-projects inert, fail-open, approval escape), session-start nudge (`invocationNum==0` injection, later/malformed/worktree/lock-owner silence), and `.agents/hooks.json` registration shape.
- `bin/fm-lint.sh bin/fm-turnend-guard-agy.sh bin/fm-hardrule1-pretool-check.sh bin/fm-sessionstart-agy-nudge.sh tests/fm-turnend-guard-agy.test.sh` → clean under the pinned ShellCheck definition.
- Hook input/output contracts for `PreToolUse`, `PreInvocation`, and `Stop` verified from the installed Antigravity CLI customization docs (`~/.gemini/antigravity-cli/builtin/skills/agy-customizations/docs/hooks.md`), not assumed: `toolCall.name`/`toolCall.args` with `CommandLine` for run_command, `invocationNum`, `fullyIdle`, `terminationReason`, and the stdout decision/inject contracts.

## Live-probe checklist still owed (none of these are closed by the hermetic suite)

- [ ] Confirm the workspace `.agents/hooks.json` `Stop` hook actually fires per turn end in the CLI 1.1.22 and that `decision: continue` re-enters with the reason visible to the model (Phase 1 acceptance gate; the changelog's ordering fix suggests it fires at all).
- [ ] Confirm `fullyIdle` semantics on a live CLI session with background tasks.
- [ ] Add `agy` to `FM_HARNESS_RE`/`FM_HARNESS_NAMES` and verify `bin/fm-lock.sh`/session-start lock acquisition on an AGY primary (Phase 3 session-start dependency).
- [ ] Probe interrupt/resume for `bin/fm-control.sh` (Phase 5).
- [ ] Probe skill frontmatter (`user-invocable`, `metadata.internal`) and AGENTS.md size handling (Phases 4/6).
